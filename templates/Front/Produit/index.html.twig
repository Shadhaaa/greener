{% extends 'base.html.twig' %}

{% block title %}Produit index{% endblock %}

{% block body %}
    <h1>Produit index</h1>

    <!--<p1>Recherche</p1>
    <form id="filters">
        {%for c in categories%}
            <p><input type = "checkbox" name = "categories[]" id = "cat{{c.id}}" value = "{{c.id}}"><label for = "cat{{c.id}}">{{c.libellet}}</label></p>
        {%endfor%}

    </form>-->
    <div id = "content">
    <table class="table">
        <thead >
            <tr>
                <th>Libellet</th>
                <th>Image</th>
                <th>Categorie</th>
                <th>Prix</th>
                <th>stock Actuelle</th>
                <th>Production Mentuelle</th>
                <th>Energie</th>
                <th>Consommation</th>
                <th>Vehicule</th>
                <th>Distance</th>
                <th>Description</th>
                <th>Pollution Par Piece</th>
            </tr>
        </thead>
        <tbody>
            {% for P in results %}
                <tr>
                    <td>{{P.libelletProduit}}</td>
                    <td><img src="{{asset('images/' ~ P.imageName)}}" width="100" height="100"></td>
                    <td>{{P.categorie.libellet}}</td>
                    <td>{{P.prix}}</td>
                    <td>{{P.stockActuelle}}</td>
                    <td>{{P.productionMentuelle}}</td>
                    <td>{{P.typeEnergie.libelletEnergie}}</td>
                    <td>{{P.consommationrnEnergie}}</td>
                    <td>{{P.typeVehicule.LibelletVehicule}}</td>
                    <td>{{P.distanceVehicule}}</td>
                    <td>{{P.description}}</td>
                    <td>{{P.pollutionParPiece}}</td>
                    <td>
                        <a href="{{ path('app_produit_edit', {'idProduit': P.idProduit}) }}"style="display: inline-block; padding: 8px 16px; border: none; border-radius: 4px; transition: all 0.3s ease; text-decoration: none; color: #fff; background-color: #3498db; margin-right: 5px;">Edit/Delete</a>
                    </td>
                </tr>
                {%else%}
                <tr>
                    <td colspan="15">no records found</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    <table>
    </div>
        <tr>
            <td><a href="{{ path('app_produit_new') }}"style="display: inline-block; padding: 8px 16px; border: none; border-radius: 4px; transition: all 0.3s ease; text-decoration: none; color: #fff; background-color: #3498db; margin-right: 5px;">Create new</a></td>
        </tr>
    </table>
    {% block javascript %}
        <script src = "{{asset('js/filter.js')}}"></script>
    {%endblock%}
{% endblock %}

