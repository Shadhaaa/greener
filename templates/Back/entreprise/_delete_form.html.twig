<form method="post" action="{{ path('app_entreprise_delete', {'idEntreprise': entreprise.idEntreprise}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entreprise.idEntreprise) }}">
    <button class="btn">Delete</button>
</form>
